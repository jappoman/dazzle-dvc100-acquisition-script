# Locally generated capture/finalisation output (when a test directory is used
# inside the repository).
/test-capture/
/finalizzati/
/*.mkv
/*.log
/index.txt
/catalogo-cassette.csv

# Local recordings, analysis reports and temporary work are not project sources.
/repairs/
/diagnostics/
/staging/
/docs/analyses/
